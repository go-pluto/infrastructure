apiVersion: v1
kind: Service
metadata:
  name: backend01
  namespace: dovecot
  labels:
    app: backend
spec:
  # type: LoadBalancer
  ports:
  - port: 993
  selector:
    app: backend
  clusterIP: 10.123.250.107
